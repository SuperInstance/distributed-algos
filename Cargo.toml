[package]
name = "distributed-algos"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/SuperInstance/distributed-algos"
description = "Distributed systems algorithms in Rust — Paxos, Raft, gossip, vector clocks, consistent hashing, CAP theorem models"

[dependencies]
serde = { version = "1", features = ["derive"] }
nalgebra = "0.33"
